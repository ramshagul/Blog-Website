<!DOCTYPE html>
<html lang="en">
<head>
    <title><%= blog.title %></title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>Hello</h1>
    <h1><%= blog.title %></h1>
    <% if (blog.image) { %>
    <img src="/images/<%= blog.image %>" alt="<%= blog.title %>" style="max-width:100%; height:auto;">
    <% } %>
    <p><%= blog.body %></p>
    <div class="blog-buttons">
    <a href="/blogs" class="return-button">Return to Blogs</a>
    <a href="/blogs/<%= blog._id %>/edit" class="edit-button">Edit The Blog</a>
    <form action="/blogs/<%= blog._id %>?_method=DELETE" method="post" style="display:inline;">
        <button type="submit" class="delete-button">Delete Blog</button>
    </form>
</div>
</body>
</html>
